[← Архитектура](architecture.md) · [Back to README](../README.md) · [Конфигурация →](configuration.md)

# API Reference

## Webhook

**POST** `/webhook`

Основной эндпоинт для приёма событий от MAX.

### Формат запроса

```json
{
  "update_type": "bot_started",
  "chat_id": 1234567890,
  "user": {
    "user_id": 1234567890,
    "name": "Иван",
    "username": "ivan_petrov"
  },
  "payload": "vip"
}
```

### Обрабатываемые события

| `update_type` | Описание |
|---------------|----------|
| `bot_started` | Пользователь запустил бота (включая deep-ссылки с `?start=`) |
| `message_created` | Новое сообщение |

### Ответ

Всегда `200 OK`, даже при ошибке в обработчике:

```json
{ "ok": true }
```

### Команды бота

| Команда | Доступ | Описание |
|---------|--------|----------|
| `/start` | Все | Приветственное сообщение |
| `/setlink <key> <url> <msg>` | Админ | Создать связку |
| `/dellink <key>` | Админ | Удалить связку |
| `/links` | Админ | Список всех связок |
| `/users` | Админ | Количество пользователей |
| `<ключ>` | Все | Получить ссылку по ключу |

## Setup Webhook

**GET** `/setup-webhook?secret=ВАШ_SECRET`

Регистрирует webhook в MAX API. Вызвать один раз после деплоя.

### Параметры

| Параметр | Описание |
|----------|----------|
| `secret` | Секретный ключ из `SETUP_SECRET` |

### Ответ

```json
{
  "webhookUrl": "https://.../webhook",
  "result": { "success": true }
}
```

## Коды ответов

| Код | Значение |
|-----|----------|
| 200 | Успех |
| 400 | Неверный JSON |
| 403 | Неверный secret |

## См. также

- [Начало работы](getting-started.md) — регистрация webhook
- [Конфигурация](configuration.md) — переменные окружения
