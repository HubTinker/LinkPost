# План: Двухшаговое удаление связок + навигация «Назад»

Дата: 2026-06-08
Ветка: — (git отключён)

## Settings

- **Тестирование:** да
- **Логирование:** verbose (DEBUG)
- **Документация:** да (обязательный чекпоинт)

## Research Context

Topic: Двухшаговое удаление связок + навигация «Назад»
Decisions:
  - Удаление через [📋 Связки] → [🗑 key] → подтверждение, отдельной кнопки в меню не нужно
  - `/dellink <key>` тоже перевести на двухшаговое подтверждение
  - [❌ Нет] из контекста списка связок → обратно к списку (колбэк `links`)
  - [❌ Нет] из `/dellink <key>` → в главное меню (колбэк `back`)
  - [🔙 Назад] во всех подпредставлениях → главное меню (колбэк `back`)
  - `cancel_del` удалить (заменён на `links` / `back`)
  - `confirm_del:` переписать: удаляет напрямую, а не редиректит на `del:`

## Tasks

### Phase 1: Инфраструктура навигации

#### T1: Рефакторинг — вынести `showAdminMenu(chatId)`

- **Статус:** ✅ Выполнено
- **Файл:** `api/index.js`
- **Что сделать:** Вынести отправку админского главного меню из `handleBotStarted` в отдельную функцию `showAdminMenu(chatId)`. Заменить вызов в `handleBotStarted` на `await showAdminMenu(chat_id)`.
- **Логирование:** `console.log('[API] showAdminMenu: показано меню для чата', chatId)`
- **Зависимости:** нет

#### T2: Добавить callback `back`

- **Статус:** ✅ Выполнено
- **Файл:** `api/index.js` (в `handleCallbackQuery`)
- **Что сделать:** Новый обработчик `cb.payload === 'back'` → вызывает `showAdminMenu(chatId)`.
- **Логирование:** `console.log('[API] callback: back → главное меню')`
- **Зависимости:** T1

### Phase 2: «Назад» в подпредставлениях

#### T3: Добавить [🔙 Назад] во все подпредставления

- **Статус:** ✅ Выполнено
- **Файл:** `api/index.js`
- **Что сделать:**
  - `links` (колбэк, строка 208): после списка связок добавить ряд `[{ type: 'callback', text: '🔙 Назад', data: 'back' }]`. Если список пуст — показать кнопку назад вместо голого текста.
  - `create` (строка 219): заменить `sendMessage` на `sendMessageWithKeyboard` с кнопкой `[🔙 Назад → back]`.
  - `users` (строка 227): то же — обернуть в клавиатуру с `[🔙 Назад → back]`.
  - `broadcast` (строка 232): то же.
  - `/links` команда (строка 163): добавить `[🔙 Назад → back]` в низ клавиатуры.
- **Логирование:** — (тривиально)
- **Зависимости:** T2

### Phase 3: Двухшаговое удаление

#### T4: Двухшаговое удаление через callback (`del:` → подтверждение → `confirm_del:`)

- **Статус:** ✅ Выполнено
- **Файл:** `api/index.js`
- **Что сделать:**
  1. **Изменить `del:{key}`** (строка 240): вместо `getLink` + `delLink` + "🗑 удалена" показывает confirmation: клавиатура с `[✅ Да, удалить → confirm_del:{key}]` и `[❌ Нет → links]`.
  2. **Переписать `confirm_del:{key}`** (строка 248): вместо рекурсивного вызова `del:` (баг со `slice(11)`) — напрямую вызывает `delLink(key)`, отправляет `🗑 Связка "{key}" удалена.` с `[🔙 Назад → back]`. Исправить извлечение ключа на `slice('confirm_del:'.length)`.
  3. **Удалить `cancel_del`** (строка 236): убрать блок `if (cb.payload === 'cancel_del')`.
- **Логирование:**
  - `console.log('[API] del: confirmation requested for key', key)`
  - `console.log('[API] confirm_del: deleted key', key)`
- **Зависимости:** T3 (чтобы `links` уже имел [🔙 Назад])

#### T5: Двухшаговое удаление через `/dellink <key>`

- **Статус:** ✅ Выполнено
- **Файл:** `api/index.js` (в `handleMessage`)
- **Что сделать:** Вместо немедленного удаления — показать confirmation с `[✅ Да, удалить → confirm_del:{key}]` и `[❌ Нет → back]`. Если ключ не найден — как сейчас, `❌ Ключ не найден.`
- **Логирование:** `console.log('[API] /dellink: confirmation requested for key', key)`
- **Зависимости:** T4 (использует тот же `confirm_del:`)

### Phase 4: Качество

#### T6: Обновить тесты

- **Статус:** ✅ Выполнено
- **Файлы:** `test/handler.test.js`, опционально `test/storage.test.js`
- **Что сделать:**
  1. Исправить существующий тест `del:` — теперь он проверяет confirmation, а не удаление
  2. Добавить тест `confirm_del:` — проверяет, что удаление происходит
  3. Добавить тест `back` — проверяет, что приходит меню с кнопками
  4. Добавить тест `links` (пустой и непустой список) — проверяет кнопку [🔙 Назад]
  5. Добавить тест для `/dellink` — проверяет confirmation с кнопками
  6. Добавить тест `cancel_del` — убедиться, что больше не обрабатывается (опционально)
  7. Добавить тест `create`, `users`, `broadcast` — проверяют кнопку [🔙 Назад]
- **Паттерн:** как в существующих тестах — мок `global.fetch`, проверка `fetchCalls.find()`, `node:test` + `node:assert/strict`.
- **Логирование:** — (тесты)
- **Зависимости:** T4, T5

#### T7: Документация (docs checkpoint)

- **Что сделать:** Запустить `/aif-docs`, обновить документацию под новые callback-колбэки и навигацию.
- **Зависимости:** T6 (после всех изменений)

## Commit Plan

Проект без git — единый чекпоинт после всех задач. Рекомендуемая структура коммита (если git будет включён позже):

1. `refactor: extract showAdminMenu helper and add back callback`
2. `feat: add back navigation to all sub-views`
3. `feat: two-step delete confirmation (callback + command)`
4. `test: update tests for delete flow and navigation`
5. `docs: update documentation for new navigation`
